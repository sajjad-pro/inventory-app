# كيف تحصل على APK جاهز يعمل على موبايلك (بدون تثبيت أي برنامج)

## الخطوة 1: أنشئ حساب GitHub (إذا لم يكن لديك)
اذهب إلى github.com وأنشئ حساباً مجانياً.

## الخطوة 2: أنشئ مستودع (Repository) جديد
1. اضغط على "New repository"
2. اسمه مثلاً: `inventory-app`
3. اجعله Public أو Private (كلاهما يعمل)
4. لا تضف README أو .gitignore (سنرفعها نحن)

## الخطوة 3: ارفع ملفات المشروع
1. فك ضغط ملف `inventory_app.zip` الذي أرسلته لك
2. في صفحة المستودع على GitHub، اضغط "uploading an existing file"
3. اسحب **كل محتويات** مجلد `inventory_app` (وليس المجلد نفسه) وأفلتها
   - تأكد أن مجلد `.github` رفع أيضاً (قد يكون مخفياً، فعّل إظهار الملفات المخفية عند السحب من الحاسوب)
4. اكتب أي رسالة commit واضغط "Commit changes"

## الخطوة 4: أضف الخط العربي (ضروري لظهور النصوص في PDF)
1. حمّل خط Cairo من: https://fonts.google.com/specimen/Cairo (زر Download family)
2. من الملفات المضغوطة، خذ ملف `Cairo-Regular.ttf`
3. ارفعه داخل مجلد `assets/fonts/` في مستودعك على GitHub (نفس طريقة الرفع بالخطوة 3)

## الخطوة 5: شغّل البناء التلقائي
1. اذهب لتبويب **Actions** أعلى صفحة المستودع
2. ستجد "Build Android APK" يعمل تلقائياً (لأنك رفعت كوداً على main)
   - إذا لم يبدأ تلقائياً: اضغط عليه من القائمة اليسرى ثم "Run workflow"
3. انتظر 5-8 دقائق حتى تظهر علامة ✅ خضراء

## الخطوة 6: حمّل ملف APK
1. اضغط على البناء الذي اكتمل (✅)
2. انزل لأسفل الصفحة إلى قسم **Artifacts**
3. اضغط على `inventory-app-apk` لتحميله (سينزل كملف zip يحتوي على app-release.apk)

## الخطوة 7: ثبّت التطبيق على موبايلك (Android)
1. انقل ملف `app-release.apk` إلى موبايلك (عبر رابط تحميل، Google Drive، أو كيبل USB)
2. من إعدادات الموبايل، فعّل "السماح بتثبيت من مصادر غير معروفة" (Install unknown apps) لتطبيق الملفات الذي ستستخدمه لفتح الـ APK
3. افتح الملف واضغط "تثبيت"
4. التطبيق جاهز ويعمل بدون إنترنت (قاعدة بيانات محلية بالكامل)

---

## بخصوص iOS (آيفون)
بناء تطبيق iOS يتطلب حساب Apple Developer (99$/سنة) وشهادة توقيع رقمي — هذا شرط تفرضه Apple نفسها على أي تطبيق، ولا توجد طريقة مجانية للتحايل عليه.
الخيارات:
- **الأسهل:** استخدم خدمة [Codemagic](https://codemagic.io) (لديها خطة مجانية) تبني وتوقّع تطبيقات iOS وتوزّعها عبر TestFlight دون حاجة لجهاز Mac.
- إذا كان لديك جهاز Mac: افتح المشروع بـ Xcode وشغّله مباشرة على آيفونك عبر كيبل (لا يحتاج حساب مدفوع للتجربة الشخصية، فقط لتوزيعه على أشخاص آخرين).

---

## ملاحظة مهمة
بعد أول نجاح، أي تعديل تريده على التطبيق مستقبلاً: عدّل الملفات في GitHub مباشرة (أو ارفع نسخة جديدة)، والبناء سيتكرر تلقائياً وينتج لك APK محدّث في كل مرة — دون أي حاجة مني لإعادة إرسال شيء يدوياً.
